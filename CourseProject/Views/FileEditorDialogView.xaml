<UserControl x:Class="CourseProject.Views.FileEditorDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             xmlns:viewmodels="clr-namespace:CourseProject.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewmodels:FileEditorDialogViewModel}"
             prism:ViewModelLocator.AutoWireViewModel="True"

             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
       
        <TextBox Grid.Row="0" Margin="5" Text="{Binding FileData}"/>
    </Grid>
</UserControl>
